// CSInterface.d.ts
{
  export default class CSInterface {
    constructor();
    evalScript(script: string, callback?: (result: string) => void): void;
    // Aggiungi qui altre firme di metodi se ne hai bisogno
  }
}
export default class CSInterface {
  constructor();
  evalScript(script: string, callback?: (result: string) => void): void;
}
